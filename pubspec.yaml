  name: planner_dp
  description: Status DP App
  publish_to: 'none'
  version: 1.0.0+1

  environment:
    sdk: '>=3.0.0 <4.0.0'

  dependencies:
    flutter:
      sdk: flutter
    http: ^1.1.0
    intl: ^0.19.0
    mocktail: ^1.0.4
    supabase_flutter: ^2.7.0
    collection: ^1.17.0
    cupertino_icons: ^1.0.8  # Verifique a versão mais recente
    image_picker: ^1.1.2
    crypto: ^3.0.3
    flutter_launcher_icons: ^0.14.3

  dev_dependencies:
    flutter_test:
      sdk: flutter
    flutter_lints: ^2.0.0

  flutter:
    uses-material-design: true

  flutter_launcher_icons:
    # android: true
    # ios: true
    image_path: "assets/icon.png"
    web:
      generate: true
      image_path: "assets/icon.png"